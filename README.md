# ollama-model-installer
A beginner‑friendly PowerShell tool that installs custom GGUF models into Ollama. Creates the model folder, detects your GGUF, builds the Modelfile, and registers the model automatically. No file moving or manual setup required
